# Local Python checks

These scripts check hand-authored Python reference translations against expected fixture values. They do not call an LLM or execute SAS. Install the dependencies named in each script and run it with Python. Validation output records the environment used for this production pass.
